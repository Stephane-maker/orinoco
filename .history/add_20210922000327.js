if (document.readyState) {
    let bodySelector = document.querySelector("body");
    bodySelector.appendChild(MakkeDiv("teddies", "ourson"));
    bodySelector.appendChild(MakkeDiv("cameras", "camera"));
    bodySelector.appendChild(MakkeDiv("furniture", "table"));
    // MakkeDiv("teddies")
    // MakkeDiv("cameras")
    // MakkeDiv("furnitur")

}