if (document.readyState) {
    let bodySelector = document.querySelector("body");
    MakkeDiv("");
    bodySelector.appendChild(MakkeDiv(""));
    bodySelector.appendChild(MakkeDiv(""));
    // MakkeDiv("teddies")
    // MakkeDiv("cameras")
    // MakkeDiv("furnitur")

}