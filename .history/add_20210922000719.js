if (document.readyState) {
    let bodySelector = document.querySelector("body");
    bodySelector.appendChild(MakkeDiv("ourson", "teddies"));
    bodySelector.appendChild(MakkeDiv("camera"));
    bodySelector.appendChild(MakkeDiv("table"));
    // MakkeDiv("teddies")
    // MakkeDiv("cameras")
    // MakkeDiv("furnitur")

}