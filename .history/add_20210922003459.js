if (document.readyState) {

    let bodySelector = document.querySelector("body");
    bodySelector.appendChild(MakkeDiv("teddies", valeurMin));
    bodySelector.appendChild(MakkeDiv("cameras", valeurMin));
    bodySelector.appendChild(MakkeDiv("furniture", valeurMin));


}